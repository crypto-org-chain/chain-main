package types

const (
	// ModuleName defines the module name
	ModuleName = "chainmain"

	// StoreKey defines the primary module store key
	StoreKey = ModuleName

	// MemStoreKey defines the in-memory store key
	MemStoreKey = "mem_chainmain"
)
